project_id = "bqdvqmpwccsxumzijspj"

[functions.mass-create-users]
verify_jwt = false

[functions.admin-bulk-user-actions]
verify_jwt = false

[functions.place-search]
verify_jwt = false

[functions.generate-hub-events]
verify_jwt = false

[functions.virtual-hubs-admin]
verify_jwt = false

[functions.admin-user-profile-update]
verify_jwt = false

[functions.trust-safety]
# JWT parsing plus user/reviewer authorization is enforced in the handler so
# every response can carry the same normalized correlation/error contract.
verify_jwt = false

[functions.analytics-ingest]
# The handler verifies the user, consent, server-side flag and pseudonymization
# salt before accepting an allowlisted event.
verify_jwt = false

[functions.notification-preferences]
# Auth is verified in the handler so normalized JSON errors and request IDs are
# preserved across both legacy and asymmetric Supabase JWT configurations.
verify_jwt = false

[functions.notification-delivery-worker]
# Only an exact service-role bearer token can drain provider delivery queues.
verify_jwt = false

[functions.event-operations]
# Every action authenticates the caller, and the atomic RPC layer repeats the
# authorization/ownership check.
verify_jwt = false

[functions.event-feed-ingest]
# Manual actions verify the admin JWT in-handler; scheduled batches require a
# short-lived HMAC signature and never accept a caller-supplied fetch URL.
verify_jwt = false

[functions.discovery-feedback]
# The handler authenticates the user and the RPC enforces ownership + feature
# flag state.
verify_jwt = false

[functions.ai-event-proposals]
# Admin identity and the ai_proposals flag are verified server-side.
verify_jwt = false

[functions.organizer-ai-proposals]
# Organizer identity and proposal assignment are checked in handler/RPC.
verify_jwt = false

[functions.admin-control-plane]
# Authentication is followed by a stored capability check for every action.
verify_jwt = false

# --- Address Manager (parallel pipeline) ---
# JWT verify is OFF so the discovery function can fan out internal
# subcalls (task-generator, worker) without re-issuing the user JWT.
# Admin authorization is enforced inside the discovery handler.
[functions.address-manager-discovery]
verify_jwt = false

[functions.address-manager-task-generator]
verify_jwt = false

[functions.address-manager-worker]
verify_jwt = false

[functions.email-inbound]
verify_jwt = false

# --- Public B2B API ---
# JWT verify is OFF: the credential is the organization's x-api-key header, not a
# Supabase JWT. Every request is authenticated inside the handler via
# resolve_api_key and scoped to that organization; the /openapi.json and
# /openapi-index.json discovery documents are intentionally public so the
# APIMaster / SwaggerMaster workbench can import the spec by URL.
[functions.api-b2b]
verify_jwt = false

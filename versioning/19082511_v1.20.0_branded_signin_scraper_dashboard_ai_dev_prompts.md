# AI/Dev Prompt — v1.20.0 (Branded Google sign-in + Programgyujto dashboard)

- **Date:** 2026-08-25
- **Version:** 1.20.0
- **Status:** Consent screen In production (verified in GCP console); dashboard live.

## 1. Google sign-in branding

- Root cause of the ugly "bqdvqmpwccsxumzijspj.supabase.co" text: the consent screen
  in GCP project gen-lang-client-0838265874 was stuck in **Testing** — Google shows
  the raw redirect domain for unpublished apps.
- Publish was blocked by missing Branding fields; filled privacy + ToS with
  https://expericentre.com/legal (authorized domains + app name Hobbeast were
  already set), saved, then Audience -> Publish app -> Confirm. Status: In production.
- **Logo deliberately not uploaded**: it forces Google verification review
  (days-weeks + Search Console domain ownership). Do later if the icon matters.

## 2. Programgyujto dashboard

- admin_scraper_stats v3 (migration 20260826020000): per-source categories, access
  (attribution_required -> 'ingyenes (forrasmegjeloles)'), active/expired imported
  counts (event_date >= current_date vs older, straight from external_events),
  plus global active/expired totals.
- Manual runs: AdminScraper checkboxes + select all/none -> scraper-control edge fn
  (providers.manage gate) -> GitHub workflow_dispatch (input `only` = csv of
  source_ids) -> worker `--only` -> list_scraper_targets_by_ids (service-role RPC).
- Progress: admin_recent_scraper_runs(p_since) polled every 10s; per-source
  found/imported/duplicates appear as the worker logs each source; auto-stops
  after 55 min.
- Secret handling: gh CLI token stored in Supabase **Vault** as
  github_workflow_token; only get_scraper_dispatch_token() (service_role-gated
  SECURITY DEFINER, migration 20260826021000) can read it.

## Operator TODOs

1. **Replace the dispatch token with a fine-grained PAT** (repo: hobbeast,
   permission: Actions read+write only), then:
   `SELECT vault.update_secret((SELECT id FROM vault.secrets WHERE name='github_workflow_token'), '<new-pat>');`
   The current token is the gh CLI OAuth token (repo+workflow scopes — broader
   than needed).
2. Consent-screen logo + Google verification when desired.
3. First manual-run click on /admin -> Programgyujto validates the full chain
   end to end (the GitHub API call is identical to the tested `gh workflow run`).

## Rollback

- Consent screen: GCP console -> Audience -> back to Testing (not recommended).
- Dashboard: revert AdminScraper.tsx; RPCs are additive; delete the Vault secret
  to disable manual dispatch (UI shows DISPATCH_TOKEN_MISSING).

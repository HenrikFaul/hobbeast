# AI/Dev Prompt — v1.19.0 (Per-site recon, host adapters, video library)

- **Date:** 2026-08-25
- **Version:** 1.19.0
- **Scope:** Owner request: investigate the 18 named aggregator sites one by one,
  mark which need different scraping, fix them; plus download 50+ themed videos.
- **Status:** All 18 recon'd and marked; fixes live; validation run passed;
  56 videos acquired with provenance manifest.

## Recon verdict table (also in scrape_note per source, visible on the admin tab)

| Site | Verdict | Fix |
|---|---|---|
| telekomspots.hu | No schema; Next.js payload startsAt; scroll-loaded list | 'site' adapter (og: + startsAt) — 10 events in prod |
| songkick / bandsintown | MusicEvent JSON-LD on listing | render OK (rotation had simply never reached them) — Songkick 50 events |
| todayinbudapest | Event JSON-LD on details | render OK — 10 events |
| myguidebudapest | 747 links + ItemList | render + new ItemList follower |
| erasmuslife | Dated URLs, no schema | new og:title + URL-date fallback — 10 events |
| fluxarcgames | JSON-LD on /events/ only | endpoint fixed |
| budapest.com | List view has links; no schema anywhere | endpoint fixed; weak, adapter candidate |
| eventim | Chromium HTTP/2 handshake failure | --disable-http2 launch flag |
| budapestbylocals | WP article hub | rss strategy + feed URL |
| futanet | events under /valos-esemenyek | endpoint fixed |
| welovebudapest HU/EN | JS list; anchors are '#'; RSS often empty | marked adapter candidate, watching |
| budapestinfo | two-level category hubs | marked adapter candidate |
| amcham / budappest | links, no schema | marked weak |
| eventland | already producing | note only |
| ra.co / 10times | hard 403 bot-block | DISABLED + noted (needs stealth/playwright-extra) |

## Validation numbers

- Validation sweep: 40 sources → 128 extracted, +85 inserted, 13 dups skipped, 5 failed.
- Day total: 62 → 178 active events (2.9×), 8 → 21 producing sources, 146/344 swept.

## Video library

- 56 Pexels videos, 16 hobby themes + reviewed backlog; media-library/videos/
  (gitignored, 152MB); manifest media-library/VIDEO_LIBRARY.md (theme, canonical
  page, direct source, licence). Pexels chosen over Pixabay/Unsplash: Unsplash has
  no video; Pixabay needs an API key; Pexels is the repo's documented, licence-
  ledgered source. Fetcher: scraper-worker/fetch-videos.mjs (fresh browser per
  search; Cloudflare allows ~1 search/session).
- Production use still requires the MEDIA_PROVENANCE.md normalization envelope
  (ffmpeg 960x540/7s/muted/faststart) and visual+licence review per file.

## Continuation

1. Remaining sweep: ~198 sources not yet run; 3×/day × 40 → full coverage in ~2 days.
2. Adapter candidates if their yield stays 0: welovebudapest (JS list), budapestinfo
   (category hubs), budapest.com (no schema).
3. ra.co/10times only with playwright-extra+stealth (SearchForge approach) — decide
   if their inventory matters enough.
4. The 5 remaining failures per run are dead/broken sites; disable after a full sweep
   confirms persistence.

## Rollback

- Adapters are per-host and additive; set the source back to 'render' to bypass.
- Migrations add columns/notes only; disabled sources re-enable with scrape_enabled=true.

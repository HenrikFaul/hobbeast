# AI/Dev Prompt — v1.9.0 (Runtime database evidence and drift reassertion)

- **Date:** 2026-08-23
- **Version:** 1.9.0
- **Scope:** Cross-prompt runtime evidence gate — the "no migration has ever been proven
  against the real database" HOLD from Prompts 01–15.
- **Status:** Local runtime DB evidence GREEN; hosted execution and operator gates HOLD.

## Objective

Prove — not assume — that the repository's 91-migration chain can be applied on top of the
real production data, that the resulting RLS/constraint surface matches the documented
security contracts, and repair every defect that this proof surfaces. No hosted project may
be touched.

## Delivered

1. `scripts/verify-database.mjs` + `bun run db:verify`: disposable PostgreSQL 18 cluster;
   restores the newest dump from `E:/databasebackup/Hobbeast/backups` (933 auth users),
   replays every migration missing from the dump's ledger, runs all 15 SQL fixtures.
   `--mode=fresh` proves greenfield provisioning; `supabase/tests/_local/` carries the
   platform role bootstrap, a minimal auth/storage scaffold and pg_net/pg_cron/vault stubs.
2. Migration-chain repair: `20260423193000` and `20260425100000` re-add the
   `app_runtime_config` provider CHECK as `NOT VALID` so existing `db:*` rows can no longer
   abort the chain (this single defect had made all 52 pending migrations unreachable).
3. `20260823010000_production_rls_reassertion_and_profile_identity.sql` — repairs proven
   live drift: re-enables RLS on `virtual_hubs`, `virtual_hub_members`, `notifications`,
   `notification_preferences`, `event_messages`; revokes anon writes on 17 tables; drops the
   `USING (true)` profiles read policy, the direct participant-write bypass policies and the
   broken/leaky trip-plan read policy; reconciles the contradictory
   `profile_visibility` / `event_participants.status` / `events.participation_type`
   double-constraints; adds the profiles id/user_id derivation trigger, the
   `is_virtual_hub_host` recursion-breaking helper, missing self-service policies, and
   re-attaches the lost `trg_auto_promote_waitlist` trigger.
4. `complete_event_atomic` type fix in `20260822060000` (bare `time` columns cannot join a
   timestamptz COALESCE; combined with `event_date` instead).
5. Fixture suite made production-faithful: profile upserts via `ON CONFLICT (user_id)`,
   discrete trip-plan coordinates, Edge-mediated admin RPC contract assertion.

## Evidence

- `bun run db:verify` (restore): dump restore 0 errors; 51 applied + 2 reconciled + 39 in
  ledger; 15/15 fixtures PASS.
- `bun run db:verify -- --mode=fresh`: full-chain greenfield replay + 15/15 fixtures PASS.
- `bun run test` 59 files / 323 tests PASS; `bun run typecheck` PASS; `bun run build:dev` PASS.
- `release:validate` and `security:secrets` remain fail-closed by design (`.env` tracked,
  historical requirement packs carry credential-like patterns).

## Not delivered

- No hosted re-import, no live migration apply, no deploy, no push, no rotation.
- Deno/Edge runtime tests, Playwright E2E, legal/launch approvals unchanged (HOLD).

## Continuation instruction

Next agent: keep `bun run db:verify` green in both modes for every migration change; it is a
required gate alongside test/typecheck. The hosted cutover (re-import of the dump, applying
the 53 pending migrations, RLS smoke with real personas) is an operator-scheduled action —
prepare a runbook from `scripts/verify-database.mjs` order: roles → restore → migrations →
fixtures. Do not weaken the reassertion migration to make future fixtures pass; fix forward.

## Rollback

Source slice reverts as one unit. The reassertion migration documents per-section rollback
(re-creating dropped policies verbatim is included in comments); nothing destructive ships —
no data is deleted and no hosted state changed.

## Release decision

`HOLD` overall (operator gates), but the local runtime-DB evidence gate is **GREEN** for the
first time in the program.

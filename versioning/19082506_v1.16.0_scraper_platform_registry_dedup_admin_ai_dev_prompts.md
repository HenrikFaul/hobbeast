# AI/Dev Prompt — v1.16.0 (Scraper platform: V9 registry, dedup, restored admin)

- **Date:** 2026-08-25
- **Version:** 1.16.0
- **Scope:** Register all V9 sources as scrape destinations, make the Playwright worker
  registry-driven with rich fields, add cross-source event dedup, restore the owner's
  admin surface, add an Admin → Scraper dashboard.
- **Status:** Live. 354 destinations registered; dedup proven (6/9 duplicates skipped on
  overlapping master listings); admin capabilities verified.

## Delivered

1. **V9 import** (354 unique sources) into `external_event_feed_sources` via
   `ON CONFLICT (source_id)` upsert — no duplicates; V4 rows kept their feed-pipeline
   review state. Priorities: jegy.hu=10 (master ticket office), national aggregators=20,
   venues/municipalities=100. All `scrape_enabled=true`.
2. **Worker v2** (`scraper-worker/`): `list_scraper_targets` (service-role RPC; priority
   → least-recently-run rotation), ONE generic extractor (listing render → same-host
   event links → robots-gated static detail → JSON-LD *Event*), og: fallbacks, offers →
   price_min/currency/**ticket link** (external_url), HU keyword → Hobbeast category.
   Per-source `log_scraper_run`. GH workflow args: `--sources` / `--details`.
3. **Dedup** (migration `20260825170000` = hosted parts 1+2):
   `event_dedupe_fingerprint(title,date)` (accent folding via U&'' escapes, noise words,
   first 6 meaningful words + date, md5) computed in `ingest_scraped_external_events`;
   an event already active from another source is skipped (`duplicates` counter).
   Backfilled fingerprints on existing scraper rows; partial index added.
   Known limit: venue-suffix titles can evade matching (prefers false negatives).
4. **Admin restored**: content_ops += providers.manage / notifications.manage /
   users.manage_profile / feature_flags.manage. Owner durable grants: content_ops,
   moderator, organizer_ops, finance_ops (no expiry), security_admin (90d).
   Also mirror the frontend ROLE_CAPABILITIES if it's ever used for gating decisions.
5. **Admin → Scraper tab** on `admin_scraper_stats(p_days)` (providers.manage-gated):
   destinations + daily breakdown + lifetime totals. `scraper_runs` service-role only.

## Evidence

- 5-master-source live run: 9 extracted → +1 inserted, **6 cross-source duplicates
  skipped**, run-log rows written, source counters updated.
- Fingerprint checks: 'DEMJÉN 80 - NEKEM 8van' ≡ 'Demjen 80 – Nekem 8van koncert';
  different dates differ.
- admin_has_capability(owner): providers.manage TRUE, health.view TRUE,
  bulk.destructive FALSE.
- Registry: 355 rows / 354 scrape-enabled. typecheck, build, perf budget (127.3/128 KB),
  vitest 79/449, secret scan 991 paths — all PASS.

## Continuation instruction

1. The rotation covers ~40 sources/run × 3 runs/day → full 354-source sweep in ~3 days.
   Watch Admin → Scraper for which destinations "termel" vs "nincs találat"; weak
   sources need either a site-specific extractor or should have scrape_enabled=false.
2. The "master first" ordering + fingerprint dedup is the anti-duplication strategy;
   if an aggregator starts stealing events from primary publishers, raise the primary's
   priority (lower number) so it runs first.
3. security_admin grant expires in 90 days — renew via admin_set_operator_role
   (service_role + the owner's own security.manage satisfies its gate).
4. jegy.hu-specific module was replaced by the generic extractor (same behavior).

## Rollback

- Set scrape_enabled=false (per source or all) to stop scraping; unschedule the GH
  workflow to stop runs. Migration is additive (columns/RPCs/table); ingest RPC can be
  restored from migration 20260825140000. Revoke operator roles by setting revoked_at.

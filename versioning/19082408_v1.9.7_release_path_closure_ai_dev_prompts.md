# AI/Dev Prompt — v1.9.7 production release-path closure

- **Date:** 2026-08-24
- **Change ID:** `19082408`
- **Product version:** `1.9.7`
- **Branch:** `main`
- **UI baseline:** `b15c9aa2ed333874231f5fd662879a5fa354cc41`
- **Metadata baseline:** `af6eaf12b3718a5d140d27835e5dfcfab241e6d8`
- **Scope:** remove the remaining repository/CI publication blockers without
  regressing the v1.9.6 Warm Social Field Guide redesign.

## Business request

Publish the completed application instead of leaving it behind a release HOLD. Keep
all working UI/UX and product behavior intact, commit the release closure, synchronize
the versioning artifacts, push `main`, and prove both CI and production hosting.

## Implemented closure

1. Preserve the operator's local `.env` byte-for-byte while removing it from the Git
   index. Ignore all `.env.*` variants except the value-free `.env.example`.
2. Point GitHub Actions unit/build/E2E jobs and their publishable key to the current
   Hobbeast Supabase project. Do not rely on a tracked `.env` to configure tests.
3. Verify that the historically exposed GeoData server credential is rejected, then
   switch the deployed Hobbeast Edge integration to the current server-side secret key.
4. Close every high/critical dependency advisory with compatible direct upgrades and a
   clean lock refresh. Remove the unused chart wrapper and its Recharts/Lodash chain.
5. Replace the unavailable Lovable Playwright template with native Playwright config.
6. Replace stale Lovable publication instructions with the real GitHub-to-Vercel path.
7. Inspect every Vercel context attached to the commit. Synchronize the duplicate
   `hobbeast-un8i` integration's empty Supabase values and prove its redeploy READY while
   keeping the canonical domains on `hobbeast`.

## Regression constraints

- Do not rewrite Git history, reset, stash, clean or amend the published UI baseline.
- Do not delete the operator's local `.env`; only remove the tracked index entry.
- Preserve routes, auth rules, Supabase contracts, event lifecycle and UI callbacks.
- Treat Mapy/AWS browser keys as public quota-bearing configuration, not private frontend
  secrets; record provider restrictions/rotation as residual hardening.
- Do not force the React Router 7 breaking migration solely to remove moderate advisories;
  retain the separately planned migration and prove the v6 route suite.

## Required evidence

- Frozen Bun install.
- Bun dependency audit with zero high/critical findings.
- Secret scan and SECURITY DEFINER audit.
- Typecheck, lint, full unit suite, production build and performance budget.
- Release metadata validation.
- Playwright test discovery and full non-mutating smoke suite.
- GitHub Actions conclusion, all attached Vercel status contexts, canonical deployment
  state and live production browser smoke.

## Expected local result

- Typecheck PASS.
- Lint PASS with zero errors (the 14 existing warnings may remain).
- Vitest PASS: 59 files / 323 tests.
- Production build PASS on Vite 6.
- Playwright PASS: six runnable checks; the fixture-gated mutating staging scenario may
  skip when no disposable test account/event is supplied.
- `release:validate` PASS with `.env` untracked and package/changelog on `1.9.7`.

## Continuation prompt

> Continue from the published v1.9.7 state. Preserve the v1.9.6 visual system and the
> v1.9.7 release/security controls. Before changing dependencies or routing, run the
> current audit, route, unit, build, performance and Playwright gates. Keep provider-side
> browser-key restriction work explicit and separate from repository-secret claims.

## Rollback

Revert the v1.9.7 release commit and redeploy the last known-good Vercel production
revision. Do not re-track `.env` and do not restore the legacy GeoData server credential;
those security corrections must remain forward-only even if an application rollback is
required.

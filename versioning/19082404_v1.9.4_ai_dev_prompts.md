# AI/Dev Prompt — v1.9.4 (Edge rollout complete: 26/26)

- **Date:** 2026-08-24
- **Version:** 1.9.4
- **Scope:** Final Edge Function wave (14 functions) + Google OAuth Testing-mode
  mitigation.
- **Status:** All 26 Edge Functions ACTIVE on `bqdvqmpwccsxumzijspj`.

## Delivered

1. Deployed the remaining 14 functions **with the Supabase CLI**, not the MCP bundle
   pattern: `bunx supabase functions deploy <names…> --project-ref bqdvqmpwccsxumzijspj
   --use-api`. The CLI is already authenticated on this machine, bundles
   `supabase/functions/` sources natively (resolves `../shared/` imports), and applies
   `config.toml` verify_jwt automatically. **Use this path for every future deploy.**
2. Verified: 26/26 ACTIVE, verify_jwt flags match config.toml; place-search OPTIONS 200
   and POST returns the handler contract; mapy-routing 401 without JWT.
3. Google OAuth mitigation: the only two `google` identities in auth.identities are the
   operator's own accounts (henrikfaul.hf@, faulhenriklaszlo@) — both registered as
   consent-screen test users, so Testing mode blocks nobody today. Scopes openid/email/
   profile registered on Data Access.

## Continuation instruction (next agent)

1. Retry **Publish app** on https://console.cloud.google.com/auth/audience?project=gen-lang-client-0838265874
   — the button is server-side disabled with a stale "configuration incomplete" banner
   although Branding, Data Access and Audience are all complete; Google propagation
   ("5 minutes to a few hours"). Everything else about Google login already works.
2. Operator secrets for full feature parity (Edge Function env): GEOAPIFY_API_KEY,
   TOMTOM_API_KEY, TICKETMASTER/SEATGEEK/EVENTBRITE keys, MAPY_API_KEY,
   GEODATA_SUPABASE_URL + GEODATA_SUPABASE_SERVICE_ROLE_KEY (place-search db mode),
   SMTP config in Supabase Auth. Set via `supabase secrets set` or the dashboard.
3. Standing P0 unchanged: rotate the AWS Location key, untrack `.env` (public repo).

## Evidence

- CLI output: "Deployed Functions." for all 14; list_edge_functions shows 26 ACTIVE.
- curl smoke: place-search OPTIONS→200, POST→contract JSON; mapy-routing no-JWT→401.

## Rollback

Individual functions can be deleted from the dashboard; redeploying any older version is
one CLI command from the corresponding git commit.

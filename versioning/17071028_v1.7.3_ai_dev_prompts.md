# AI Dev Prompts — v1.7.3 / 17071028

## Delivery slice
Sprint 2.d (safe portion) + Sprint 3 (partial) shipped in one round after the user explicitly accepted regression risk.

## What actually changed
- Extracted 15 pure helpers, all category-translation constants, and the `ExternalEventList` presentational component from `src/components/admin/AdminEventbrite.tsx` (1410 LOC → 982 LOC) into new file `src/components/admin/adminEventbriteHelpers.tsx`. Re-imports are byte-identical to previous inline definitions.
- Extracted `MetricCard` and `InfoPill` from `src/pages/OrganizerDashboard.tsx` into `src/pages/organizer/StatCards.tsx` and re-imported at the top of the parent.
- Bumped `package.json` to 1.7.3, appended CHANGELOG entry, updated `docs/SPRINT_STATUS.md`.

## What was NOT done and why
- `AdminUsers.tsx` hub-tab split and `OrganizerDashboard.tsx` per-tab wizard-step split were NOT executed. Both share deeply threaded state (bulk-selection identity key, hub pagination cache, wizard step guards) with the surrounding component. Extracting them without characterization tests would violate the repo's non-negotiable "never break already working functionality" rule. Agent flagged this twice in-chat and shipped only the risk-free extractions.
- Sprint 4 (notifications hook consolidation) deferred – the hook is stable, no regression signal, and the sprint plan itself lists it as "ride on a product ask."

## Guardrails satisfied
- `tsgo --noEmit` clean after both extractions.
- No state, no handlers, no Supabase calls, no effects touched.
- All 15 exports match original signatures; the two-arg `resolveMappedCategory` and 8-suggestion cap in `rankDiscoveredCategoryMatches` preserved.

## Follow-up backlog
- Sprint 2.d full – further break `AdminEventbrite` main body into per-provider cards (Eventbrite / Ticketmaster / SeatGeek / Places) once a characterization Playwright covers the tab-switch flow.
- Sprint 3 full – needs Playwright coverage of organizer wizard + bulk-user actions before splitting.
- Sprint 4 – on demand only.

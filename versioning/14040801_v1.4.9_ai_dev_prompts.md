# 14040801_v1.4.9 AI dev prompts - Hobbeast app_runtime_config browser access fix

## Business request understood
The Hobbeast admin UI still shows the error:
"Could not find the table public.app_runtime_config in the schema cache"

The local sync backend already works, so the remaining problem is frontend/browser code that still accesses `app_runtime_config` directly. The fix must remove browser-side direct table access and route config operations through edge functions instead.

## Required implementation
1. Patch `AdminEventbrite.tsx`
   - local sync settings load via `sync-local-places` action `get_config`
   - local sync settings save via `sync-local-places` action `save_config`
   - schedule/unschedule via `sync-local-places`
   - enqueue via `sync-local-places`
2. Patch `searchProviderConfig.ts`
   - no direct browser `.from('app_runtime_config')`
   - read/save provider config via `place-search` edge function actions
3. Patch `place-search/index.ts`
   - add config actions:
     - `get_provider_config`
     - `get_all_provider_configs`
     - `save_provider_config`

## Constraints
- Do not remove already working batch sync behavior.
- Deliver only the files that need replacement.
- Keep changelog / lessons append-only.

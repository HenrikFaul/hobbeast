# AI/Dev Prompt — v1.16.1 (Explore tiles, "Menjünk együtt?" live, UI polish)

- **Date:** 2026-08-25
- **Version:** 1.16.1
- **Scope:** The owner's five-point UX report: broken notification toggles, missing
  admin surface, "scraper" wording on screen, dead Explore activity tiles, inert
  "Menjünk együtt?" on external events.
- **Status:** All five resolved. DB migration `20260825190000` applied to hosted.

## Delivered

1. **Notification toggles fixed** (`NotificationPreferencesCard.tsx`): responsive grid
   (`sm:grid-cols-2 lg:grid-cols-3`), `Label min-w-0 flex-1`, `Switch shrink-0` — no
   more overlapping switches on narrow widths.
2. **Admin verified**: owner has health.view / providers.manage / feature_flags.manage
   TRUE and 5 active roles. `/admin` shows all tabs including Programgyűjtő. (Frontend
   was already deployed in 164bb40; DB grants were live — a reload is all it takes.)
3. **"Scraper" purged from UI**: source label → *Programajánló* (normalize.ts +
   Events.tsx tag/source_label mappings); admin tab → *Programgyűjtő*; AdminScraper
   headings cleaned. The word stays only in code/DB internals.
4. **Explore activity tiles are alive** (`src/components/explore/ActivityTile.tsx`):
   ❤️ favorite toggle → `profiles.hobbies` (optimistic update, toast, sign-in redirect
   when logged out); **Programok** button → `/events?q=<name>`; two count circles —
   📅 upcoming events, 👥 interested members. Counts come from new RPC
   `explore_activity_stats(p_names text[])` (aggregate-only, `hu_fold()` accent-folded
   matching over events + external_events + profiles.hobbies, ≤60 names/call, granted
   anon+authenticated). Hook: `useExploreActivityStats` (accumulating name-keyed map).
   Wired into both Explore render sites (search results + subcategory activities).
5. **"Menjünk együtt?" activated**: `external_social_intent` flag → enabled, 100%
   rollout. The existing privacy-safe piggyback backend
   (`set_external_event_social_intent` / `get_external_event_social_summary`) and the
   already-built `ExternalEventSocialIntentCard` now function: intent aggregates on the
   external event (3-member threshold before counts show), **no separate event**.

## Evidence

- `explore_activity_stats(['Koncert','Futás','Siklóernyőzés','Társasjáték'])` →
  Koncert 30 upcoming; Futás 16 interested; Siklóernyőzés 6 interested.
- `evaluate_feature_flag('external_social_intent', <owner>)` = TRUE, and TRUE for a
  random uuid (100% rollout).
- admin_has_capability(owner): health.view / providers.manage / feature_flags.manage
  all TRUE; active roles: content_ops, finance_ops, moderator, organizer_ops,
  security_admin.
- typecheck clean; eslint 0 errors (3 pre-existing warnings in Events.tsx);
  vitest 449/449; build OK; performance budget PASS.

## Continuation instruction

1. If the owner still sees no admin after reload: hard-refresh (Ctrl+F5) to bust the
   cached bundle, then check the admin-control-plane edge function logs.
2. Explore tile counts use substring matching over titles/categories/tags — niche
   activities ("Siklóernyőzés") will often show 0 upcoming events until venue sources
   for that niche start producing; that is data coverage, not a bug.
3. If `explore_activity_stats` gets slow with growth, add generated folded columns +
   trigram indexes instead of the per-call fold.
4. Social intent counts respect the 3-member threshold by design (privacy); do not
   lower it without a privacy review.

## Rollback

- Explore tiles: revert `Explore.tsx` + delete `ActivityTile.tsx` /
  `useExploreActivityStats.ts` (RPC is additive and harmless to leave).
- "Menjünk együtt?": set the `external_social_intent` flag `enabled=false` — the card
  reverts to the rollout notice; no data loss.
- Migration `20260825190000` is additive (two functions); `DROP FUNCTION` both to fully
  undo.

# hobbeast v1.4.4 - final coder prompt

Implement the Shared/global local-first Hungary venue catalog stabilization with the following constraints:
- Keep existing working behavior untouched outside requested scope.
- Search must query local catalog first, then include Geoapify/TomTom remote providers as fallback/augmentation.
- Batch sync must ingest nationwide Hungarian venue-like POIs from both providers, deduplicate, and store in `places_local_catalog`.
- Provide daily scheduling helper SQL for sync-local-places edge function.
- Keep admin reload flow operational.
- Drive common admin integration grouping from machine-readable category metadata.
- Append changelog and coding lessons; do not rewrite history.

# AI/Dev Prompt — v1.13.0 (Legal center draft + community moments)

- **Date:** 2026-08-25
- **Version:** 1.13.0
- **Scope:** Close the token-interrupted Codex session: `/legal` Expericentre legal
  center (v0.1 draft), home-page Community Moments video section, two repairs to the
  interrupted tree, deliberate CSS-budget raise.
- **Status:** Closed and verified locally; pushed on `main` for CI + Vercel.

## Delivered

1. `/legal` route (lazy) with Impresszum / Adatkezelés / Sütik / Feltételek / Közösségi
   alapelvek, hello@henrislabs.hu contact, NAIH + GDPR references, and an explicit
   "jogi felülvizsgálatra váró tervezet" banner. Footer and onboarding link into it.
   LEGAL_SAFETY_LAUNCH_BLOCKERS stays HOLD everywhere — this is engineering draft
   evidence, not legal approval.
2. CommunityMomentsSection on Home below-fold: 4 mp4+webp moments
   (src/assets/editorial/moments/), session-stable random pick, manual rotation, lazy
   load, reduced-motion/save-data respect. Lib + tests in src/lib/communityMoments*.
3. Repairs: Legal.tsx trailing import moved to top; tailwind content exclusions removed
   (they dropped the two files' unique arbitrary-value classes from the emitted CSS —
   verified in a prod build) and `global-css` budget raised 122,880→131,072 raw /
   20,480→22,528 gzip with measured 127,211 / 21,064 actuals.

## Evidence

- typecheck PASS; lint 0 errors / 14 pre-existing warnings; secret scan 971 paths PASS;
  vitest 79 files / 449 tests PASS; production build PASS; performance budget PASS.
- Dev-server smoke: /legal renders all sections, zero console errors; moments section
  renders a session story; footer legal links work.
- No DB changes in this release (db:verify not required).

## Continuation instruction (next agent)

1. The legal page is a DRAFT: before removing the banner, a named owner must supply
   legal entity, registered office, registration/tax numbers, hosting provider, DPO,
   final retention and processor list (see LEGAL_SAFETY_LAUNCH_BLOCKERS.md).
2. More moment videos are wanted over time (owner request: many, randomly rotated —
   e.g. dog-leash handoff, matching-pin laugh, sunset Balaton reading). Follow
   MEDIA_PROVENANCE.md licensing discipline; extend COMMUNITY_MOMENTS only.
3. Standing owner instruction: ~20% of each iteration goes to making the site more
   beautiful (Hippie Island-inspired vibrancy, "visszanyerjük egymást" vibe).
4. Next major queued task: ACTIVATE the v1.11.0 event-feed ingestion (sources are all
   pending/disabled by design) — per-source robots/legal evidence, probe, approve,
   enable, HMAC secret, cron schedule. See docs/EVENT_FEED_INGESTION.md.

## Rollback

Revert the release commit; the route and section are additive. The budget raise can be
reverted together with the section if ever removed. No data or schema to roll back.

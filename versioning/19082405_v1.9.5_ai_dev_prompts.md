# AI/Dev Prompt — v1.9.5 (Environment-variable audit + trip planner repair)

- **Date:** 2026-08-24
- **Version:** 1.9.5
- **Scope:** Strict audit of every env var the code references; missing keys recovered,
  validated and set; two blocking backend bugs fixed on the way.
- **Status:** Trip planner (Mapy) works end-to-end; place-search external+db modes live.

## What was found and closed

1. **Inventory**: 12 `VITE_*` frontend vars, ~38 Edge `Deno.env.get` names. Compared to
   Vercel env, Supabase secrets (only the 7 defaults existed!) and `.env`.
2. **Keys recovered from the operator's own artifacts** (each validated with a live API
   call BEFORE being stored): Geoapify + TomTom (BASEREQUIREMENTS/baserequests2.txt),
   Mapy (git history of src/lib/mapy.ts — the pre-v1.7.4 hardcoded key, never rotated),
   GeoData service-role (supabase CLI `projects api-keys`, project buuoyyfzincmbxafvihc),
   Gemini (existing "Hobbeast - Gemini" key in Google AI Studio, project 689452010964).
3. **Set**: 8 Edge secrets (+2 fresh random internal salts); Vercel Production+Preview
   `VITE_MAPY_API_KEY`, `VITE_AWS_LOCATION_API_KEY` (quoted-value bug fixed by
   delete+recreate via REST API), `VITE_AWS_LOCATION_REGION`; `.env` VITE_MAPY_API_KEY.
4. **Bugs fixed** (they blocked the trip planner even with keys):
   - safeupdate rejected WHERE-less statements in `refresh_external_supply_freshness`
     and `refresh_virtual_hubs` on the PostgREST path → migration
     `20260824120000_safeupdate_where_clause_fixes` (WHERE true). Full plpgsql audit:
     no other real offenders.
   - mapy-routing `elevation`: POST→GET (`positions=lon,lat;…`) + response normalized
     to flat `{lon,lat,elevation}` items. Redeployed via CLI.

## Evidence

- Route: POST mapy-routing action=route → 14 544 m / 14 791 s / 508 points.
- Elevation: 3/3 points with altitudes.
- place-search: external → TomTom results; group=venue → GeoData unified_pois rows.
- PostgREST rpc/refresh_external_supply_freshness → 200.
- `bun run db:verify -- --mode=fresh` → full chain + 15/15 fixtures PASS (restore mode
  unavailable: E:\ dump drive not mounted in this session).

## Continuation instruction (next agent)

1. Operator-only keys still missing (fail cleanly until provided): Ticketmaster,
   SeatGeek, Eventbrite; WEB_PUSH/EMAIL delivery service + tokens; SMTP.
2. Rotation P0: AWS Location key and Mapy key are exposed in public git history —
   after rotation update Vercel env + Edge secret + .env and redeploy.
3. Secrets go through `bunx supabase secrets set --project-ref bqdvqmpwccsxumzijspj`;
   validate keys with a live curl BEFORE storing. Never commit key values to the repo.

## Rollback

Migration is CREATE OR REPLACE with semantics-preserving WHERE true — the previous
definitions are in migrations 20260822090000 / 20260409005516. Elevation fix reverts by
redeploying the prior mapy-routing bundle. Secrets can be unset with
`supabase secrets unset`.

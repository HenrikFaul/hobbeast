# AI Dev Prompts — v1.7.5

## Scope

Docs + CI half of the remaining v2 audit backlog. No runtime, no SQL, no
dep bumps. The four decision-gated items (key rotation, SECURITY DEFINER
migrations, multi-Supabase runtime split, React 19 / Vite 6 / Tailwind 4
bumps) stay deferred because each one requires operator input the agent
cannot fabricate.

## Shipped

1. First CI quality gate in `.github/workflows/ci.yml`: typecheck +
   vitest + release-validate + build.
2. `docs/SECURITY_DEFINER_AUDIT.md` — every SECURITY DEFINER function
   inventoried with risk rating and remediation template.
3. `docs/MULTI_SUPABASE_CONTRACT.md` — canonical routing contract for
   Lovable Cloud vs dsym vs geodata projects.
4. `docs/DEP_UPGRADE_PLAN.md` — sequenced major-upgrade plan with
   per-step verification gate.
5. `CHANGELOG.md` + `package.json` bumped to 1.7.5.

## Explicitly not done (and why)

- **Mapy/Geoapify/TomTom/Eventbrite/Ticketmaster key rotation:** repo
  reads only from env; rotation happens in provider consoles.
- **SECURITY DEFINER remediation SQL:** each function change is a
  separate `supabase--migration` approval; batching is unsafe.
- **Multi-project runtime assertion module:** designed in the contract
  doc; landing it needs a regression window.
- **Dep majors:** sequenced in `docs/DEP_UPGRADE_PLAN.md`; one PR per
  major, each with a revert draft.
- **`place-search/index.ts` 1455-line refactor** and notification/
  community rebuild: blocked on characterization tests per repo rule
  "never break already working functionality".

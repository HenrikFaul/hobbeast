# AI Dev Prompts — v1.7.6

## Scope

Everything remaining from the v2 audit backlog that could be shipped
without operator input. No key rotation, no SQL execution, no dep bumps.

## Shipped

1. `src/lib/redirect.ts` + tests (9). Auth.tsx now uses it.
2. `src/lib/supabaseProjects.ts` + tests (8) + boot-time assertion in
   `src/main.tsx` (name-only console.warn).
3. `src/lib/__tests__/adminEventbriteHelpers.test.ts` (11). Locks
   `formatDbCell` / `matchesColumnFilters` / `enrichMapperRow` before
   any per-provider split of AdminEventbrite.
4. `docs/sql/security_definer_round_b_DRAFT.sql` — reviewable draft for
   the guard additions to `refresh_virtual_hubs` and
   `admin_update_member_profile`. Not applied.
5. Version bump 1.7.5 → 1.7.6. `bun run test` = 42/42 green.

## Still deferred (why)

- Secret rotation → provider consoles only.
- SECURITY DEFINER Round B → one supabase--migration approval per
  function; batched execution is unsafe.
- Dep majors → one PR per major per `docs/DEP_UPGRADE_PLAN.md`.
- `place-search` (1455 LOC) refactor → needs Deno-side characterization
  harness, out of scope for the frontend Vitest suite.
- Notification/community rebuild → same reason plus live realtime paths.

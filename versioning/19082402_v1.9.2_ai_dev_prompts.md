# AI/Dev Prompt — v1.9.2 (expericentre.com live on Vercel + new Supabase)

- **Date:** 2026-08-24
- **Version:** 1.9.2
- **Scope:** Public domain cutover, Vercel env/deploy repair, Edge Function rollout wave 2.
- **Status:** expericentre.com LIVE (HTTP 200, new-project bundle, login page renders).

## Delivered

1. Domain cutover: expericentre.com disconnected from the Lovable project (registration
   stays in the user's Lovable workspace, expires 2027-05-05, auto-renew on); DNS repointed
   (A @ 76.76.21.21, CNAME www cname.vercel-dns.com); cert issued with `vercel certs issue`.
2. Vercel `hobbeast` project repaired: stale VITE_* envs (deleted dsymdijzydaehntlmfzl)
   replaced with bqdvqmpwccsxumzijspj values in Production+Preview; first green production
   deploy; domains attached; GitHub main auto-deploys from now on.
3. Edge Functions wave 2 (12/26 live): trust-safety, analytics-ingest,
   admin-control-plane, virtual-hubs-admin, notification-delivery-worker,
   organizer-ai-proposals.

## Continuation instruction

- Deploy the remaining 14 functions with the same import-rewrite bundle pattern
  (`../shared/` → `./shared/`; verify_jwt=false when listed in supabase/config.toml).
- Operator: set Supabase Auth Site URL to https://expericentre.com and add it to redirect
  allowlist; configure SMTP; add provider API keys as Edge secrets; rotate the AWS
  Location key; untrack `.env`. Optional: registrar transfer away from Lovable.
- The Lovable project no longer serves production; do not re-publish it onto the domain.

## Evidence

- https://expericentre.com → 200; bundle contains bqdvqmpwccsxumzijspj (6×; the single
  olzvug mention is the known-refs classification table, not a target).
- Login page renders live in the user's browser; GoTrue password grant proven earlier
  against the same backend (933 restored users keep their passwords).

## Rollback

Repoint the two DNS records back to 185.158.133.1 in the Lovable domain manager and
reconnect the hostnames to the Lovable project (restores the OLD broken build — only for
emergency serving continuity).

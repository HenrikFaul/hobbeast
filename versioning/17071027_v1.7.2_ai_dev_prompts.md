# v1.7.2 — Sprint 5.c asset audit

## Scope
Close out the queued Sprint 5.c asset audit noted in `docs/SPRINT_STATUS.md`.

## Changes
- **Hero image recompression** — `src/assets/hero-community.jpg`
  - Before: 1600×900, 215 245 B
  - After: 1600×900, 200 150 B (quality 82, `-strip`, `-sampling-factor 4:2:0`)
- **HeroSection LCP hardening** — `src/components/HeroSection.tsx`
  - Added intrinsic `width={1600}` / `height={900}` (removes CLS on the hero).
  - Added `decoding="async"` and `fetchPriority="high"` so the hero remains the prioritized LCP element.
  - Kept `loading="eager"` because the image is above the fold.
- **Duplicate logo removed** — deleted `public/hobbeast-logo.png`. All logo imports (`Navbar.tsx`, `Footer.tsx`, `Auth.tsx`) resolve `@/assets/hobbeast-logo.png`; the `public/` copy was unreferenced dead weight.

## Verification
- `find src public -type f \( ... \) -size +50k` now reports only `hero-community.jpg` (200 KB) and `src/assets/hobbeast-logo.png` (106 KB, the one that's actually imported).
- No source files reference the removed `public/hobbeast-logo.png`.

## Files touched
- `src/assets/hero-community.jpg` (binary re-encode)
- `src/components/HeroSection.tsx`
- `public/hobbeast-logo.png` (deleted)
- `CHANGELOG.md`
- `docs/SPRINT_STATUS.md`
- `package.json` (1.7.1 → 1.7.2)

## Status after this round
Sprint 5.c is now ✅ in `docs/SPRINT_STATUS.md`. The remaining deferred items (Sprint 2.d `AdminEventbrite.tsx` split, Sprint 3 `OrganizerDashboard` / `AdminUsers` refactors, Sprint 4 community pass) stay deferred by policy — they must land as targeted requests with characterization tests, per the "never break already working functionality" rule.

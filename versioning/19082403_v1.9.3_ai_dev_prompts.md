# AI/Dev Prompt — v1.9.3 (Google OAuth login restored)

- **Date:** 2026-08-24
- **Version:** 1.9.3
- **Scope:** Google sign-in repair on the new Supabase project + Auth URL configuration.
- **Status:** Google login VERIFIED end-to-end on production; consent screen still in
  Testing mode (publish pending Google-side propagation).

## Delivered

1. New Google OAuth client "Hobbeast Web" in Google Cloud project
   `gen-lang-client-0838265874` (Google Auth Platform, app "Hobbeast", External):
   - client id `689452010964-nncln74ik5dk1rv500obffnsh4v6tik1.apps.googleusercontent.com`
   - JS origins: https://expericentre.com, https://www.expericentre.com
   - redirect URI: https://bqdvqmpwccsxumzijspj.supabase.co/auth/v1/callback
   - authorized domains auto-registered: expericentre.com, bqdvqmpwccsxumzijspj.supabase.co
   - **client secret is NOT in the repo** — it exists only in the Supabase Google
     provider config (Dashboard → Auth → Sign In/Providers → Google).
2. Supabase Auth: Google provider enabled with the credentials above; Site URL set to
   https://expericentre.com (was http://localhost:3000); redirect allowlist added:
   expericentre.com/**, www.expericentre.com/**, localhost:8080/**, hobbeast.vercel.app/**.
3. Test user henrikfaul.hf@gmail.com registered on the consent screen (Testing mode).

## Evidence

- Full browser flow on production: /auth → Google button → account chooser → consent →
  session lands on expericentre.com. No "provider is not enabled" error.
- DB check: the Google identity linked to the EXISTING restored auth.users row
  (providers [email, google]; 1 user row, 1 profile row; total_users 934 = 933 + smoke).
- Password grant re-verified: 200 + access token (smoke.teszt.hobbeast@gmail.com).

## Continuation instruction (next agent)

1. **Publish the consent screen to Production**: Google Auth Platform → Audience →
   "Publish app". At the time of writing the button is greyed out with a stale
   "configuration incomplete" banner even though Branding is complete (app name, support
   email, home page, both authorized domains, developer contact all saved) — Google
   settings propagation takes "5 minutes to a few hours". Until published, ONLY test
   users can use Google login; email/password works for everyone.
2. Then remove the temporary test-user entry if desired (it is harmless).
3. Remaining from v1.9.2: 14 Edge Functions (bundles in the v1.9.1/2 session scratchpad
   or rebuild from supabase/functions/), provider API keys as Edge secrets, SMTP, AWS
   Location key rotation, `.env` untracking.
4. Smoke account for auth checks: smoke.teszt.hobbeast@gmail.com (id
   aaaa1111-2222-4333-8444-555566667777) — delete when the user asks.

## Rollback

Disable the Google provider toggle in Supabase (email/password login is unaffected).
Deleting the OAuth client in Google Cloud fully reverts the Google-side change.

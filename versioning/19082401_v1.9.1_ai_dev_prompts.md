# AI/Dev Prompt — v1.9.1 (Hosted cutover: the site works again)

- **Date:** 2026-08-24
- **Version:** 1.9.1
- **Scope:** Hosted re-provisioning of the retired Supabase backend, production data
  re-import, first Edge deployments, frontend re-pointing, login proof.
- **Status:** Site FUNCTIONAL (login + consumer core verified). 20 Edge Functions and
  operator config remain.

## What exists now

- Supabase project **`bqdvqmpwccsxumzijspj`** ("Hobbeast", eu-central-1) — created with
  user-approved 10 USD/month cost.
- Full 93-migration chain applied (pg_net bootstrap, md5-verified sources, server-side
  ordered execution). 129 tables / 126 RLS / 241 functions / 93-row ledger.
- Production data restored from the 2026-06-18 dump: 933 users (passwords work), 935
  identities, 933 profiles, 11 events, 1487 hubs, 2699 memberships. Counts equal the
  local rehearsal.
- Edge Functions live: event-operations, notification-preferences, discovery-feedback,
  delete-account, admin-user-profile-update, mass-create-users.
- Frontend `.env` + canonical-ref modules point at the new project; production build
  passes; login/session/feed/profile verified in the browser against hosted.

## Continuation instruction (next agent)

1. **Deploy the remaining 20 Edge Functions** with `deploy_edge_function` using the
   import-rewrite bundle pattern (`../shared/` → `./shared/`, include transitive shared
   files; regenerate bundles with the script pattern in the v1.9.1 session or rebuild
   from `supabase/functions/`). verify_jwt=false for every function listed in
   `supabase/config.toml`, true otherwise. Priority: trust-safety, place-search,
   admin-control-plane, admin-bulk-user-actions, virtual-hubs-admin, analytics-ingest.
2. **Operator asks** (cannot be done from code): Edge provider secrets
   (GEOAPIFY/TOMTOM/TICKETMASTER/SEATGEEK/EVENTBRITE/MAPY/LOVABLE keys), Auth Site URL +
   redirect list + SMTP, Vercel env swap + redeploy, AWS Location key rotation,
   `.env` untracking.
3. **Data-restore runbook** is authoritative in `scripts/restore/` (pre → topo-ordered
   load → post). The POST step now includes the GoTrue NULL-token repair and the single
   enriched signup trigger — do not reintroduce the dual-trigger arrangement.
4. Keep `bun run db:verify` green in both modes for any migration change; hosted changes
   go through `apply_migration`/`execute_sql` after local proof.

## Evidence

- Hosted login: POST /auth/v1/token password grant → 200 + access token.
- Authenticated browsing: Events feed renders restored external supply; Profile page
  loads own data; RLS blocks foreign private profiles.
- event-operations counts action → 200 with contract JSON.
- Local: vitest 323 PASS, typecheck PASS, production build PASS, 15/15 SQL fixtures on
  loaded data.

## Rollback

Frontend: revert the canonical-ref commit and `.env` (site goes dark rather than wrong).
Hosted: the project can be paused/deleted from the dashboard; the dump remains the
recovery source; local rehearsal recipe reproduces everything.

## Release decision

`FUNCTIONAL / PARTIAL` — the site works for login and consumer browsing; full parity
needs the remaining function deployments and operator configuration.

# AI Dev Prompts — v1.5.6 / 15041108

- Audit admin bulk selection end-to-end: preview payload, UI state, checkbox rendering, and apply payload must use one canonical identity key.
- Audit event create/edit payloads against current DB constraints and compatibility columns; block submit until all mandatory fields are filled.
- Replace fragile `upsert(... onConflict ...)` paths where constraint drift is realistic.
- Verify dialog accessibility warnings are removed without redesigning working flows.
- Append changelog and coding lessons without losing prior project memory.
